# All optional for local development (the Vite dev server proxies /api to localhost:3001).

# URL of the deployed backend, e.g. https://your-backend.up.railway.app (no trailing slash).
VITE_API_URL=

# Display name of the AI interviewer (default: Maya).
VITE_INTERVIEWER_NAME=
